func is_even(x) do
    return x 2 % 0 == .
end

func is_odd(x) do
    return is_even(x) ! .
end

func main() do
    let x = 0b1_010 .
    if is_even(x) do
        print("x is even") .
    else
        print("x is odd") .
    end
end

main() .